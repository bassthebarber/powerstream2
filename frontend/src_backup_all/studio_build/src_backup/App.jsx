// This wraps your existing component.
// IMPORTANT: the import path matches what you told me (no spaces).
// If your component file is named differently, adjust the filename.
import RecordingStudio from '../../src/components/recordingstudio/RecordingStudio.jsx'

// If your studio has its own stylesheet, import it here, e.g.:
// import '../../src/components/recordingstudio/studio.css'

export default function App() {
  return <RecordingStudio />
}
