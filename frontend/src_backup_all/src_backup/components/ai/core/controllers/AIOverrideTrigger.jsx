import React from 'react';

const AIOverrideTrigger = () => {
  return (
    <button onClick={() => alert('Override Triggered')}>
      Trigger AI Override
    </button>
  );
};

export default AIOverrideTrigger;


